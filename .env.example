# Database Configuration
# You can leave these as default for local development
DB_USERNAME=liruoyu
DB_PASSWORD=password
DB_DATABASE=gateway

# SMTP Configuration (Optional but recommended to prevent warnings)
# Leave blank if not using email features initially
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# JWT Secret (Change this for production)
JWT_SECRET=your_jwt_secret_key_change_in_prod

# Frontend URL
FRONTEND_URL=http://localhost